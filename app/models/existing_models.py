"""Models are reflected from the existing PostgreSQL database.

This module is intentionally empty because the analytics repository inspects
the live schema before constructing SQLAlchemy Core queries.
"""
