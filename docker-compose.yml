services:
  faculty-analytics:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ""
    env_file:
      - .env
    environment:
      PORT: 8080
      WORKERS: 2
    restart: unless-stopped
    ports:
      - "8080:8080"
